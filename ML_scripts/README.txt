to setup anaconda environment:

conda create -n [env name] tensorflow --user

enter env:

conda activate [env name]

exit env:

conda deactivate

might need to change the python package that is being used if you get errors